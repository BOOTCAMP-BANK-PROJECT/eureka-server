server:
  port: 8089

spring:
  application:
    name: eureka-server
  cloud:
    config:
      label: main
      uri: ${CONFIG_SERVER_URI}
      username: ${SECURITY_USERNAME:root}
      password: ${SECURITY_PASSWORD:1234}